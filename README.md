# [codedeep.ly | Terminal](https://codedeep.ly)

"A terminal style website."

A fork of [m4tt73/terminal](https://github.com/m4tt72/terminal). See fork for original documentation.

## Development

Run the same lint, type-check, unit-test, and production-build validation used by
GitHub Actions:

```sh
yarn ci
```

Maintainers should require the `CI / Validate` status check in the GitHub ruleset
for the `master` branch.
